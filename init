#!/bin/rc

rfork e

fn usage{
	echo $argv0 [-b] name >[1=2]
	echo '	-b	init bare repository' >[1=2]
	exit usage
}

sub='/.git'
while(~ $1 -*){
	switch($1){
	case '-b';	sub=''
	case *;		usage
	}
	shift
}

if(! ~ $#* 1)
	usage

mkdir -p ./$1$sub
dircp /sys/lib/git/template ./$1$sub
